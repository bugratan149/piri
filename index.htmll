<!DOCTYPE html>
<html lang="tr">
<head>
<meta charset="UTF-8">
<title>Zeynep & Berkan Nişan Davetiyesi</title>

<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet">

<style>
body {
  margin: 0;
  font-family: 'Playfair Display', serif;
  background: #f6f0e8;
  color: #4a3f35;
  scroll-behavior: smooth;
}

/* GENEL */
.section {
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  text-align: center;
  padding: 20px;
}

/* AÇILIŞ */
.hero {
  background:
  linear-gradient(rgba(246,240,232,0.7), rgba(246,240,232,0.7)),
  url('https://images.unsplash.com/photo-1520854221256-17451cc331bf');
  background-size: cover;
  background-position: center;
}

.hero h1 {
  font-size: 64px;
  letter-spacing: 4px;
  color: #c8a96a;
  margin-bottom: 10px;
}

.hero p {
  font-size: 22px;
}

.scroll {
  margin-top: 30px;
  font-size: 18px;
  animation: float 1.8s infinite;
  color: #b89a5c;
}

@keyframes float {
  0% {transform: translateY(0);}
  50% {transform: translateY(12px);}
  100% {transform: translateY(0);}
}

/* TARİH SAAT */
.info {
  background: #efe4d5;
}

.card {
  background: #fffaf3;
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 30px rgba(0,0,0,0.08);
  max-width: 420px;
}

.card h2 {
  font-size: 36px;
  color: #c8a96a;
  margin-bottom: 10px;
}

.card p {
  font-size: 20px;
  margin: 5px 0;
}

/* HARİTA */
.map {
  background: #f6f0e8;
}

.map h2 {
  color: #c8a96a;
  font-size: 32px;
  margin-bottom: 20px;
}

.map iframe {
  width: 85%;
  height: 400px;
  border: 0;
  border-radius: 18px;
  box-shadow: 0 10px 25px rgba(0,0,0,0.1);
}

/* MOBİL */
@media(max-width:600px){
  .hero h1 {font-size: 40px;}
  .card {padding: 25px;}
}
</style>
</head>

<body>

<!-- AÇILIŞ -->
<div class="section hero">
  <h1>Zeynep & Berkan</h1>
  <p>Nişanımıza Davetlisiniz</p>
  <div class="scroll">Aşağı Kaydır ↓</div>
</div>

<!-- TARİH SAAT -->
<div class="section info">
  <div class="card">
    <h2>14 Şubat Cumartesi ❤️</h2>
    <p>Saat: 13:00</p>
    <p>Nişan Töreni</p>
  </div>
</div>

<!-- HARİTA -->
<div class="section map">
  <h2>Konum</h2>
  <iframe
    src="https://www.google.com/maps?q=izmir&output=embed">
  </iframe>
</div>

</body>
</html>